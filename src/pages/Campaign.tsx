import React from 'react';

/**
 * Placeholder page for the Campaign view. In the future this could be
 * replaced with an actual campaign management UI.
 */
const Campaign: React.FC = () => {
  return (
    <div style={{ paddingTop: '5rem', paddingBottom: '5rem', textAlign: 'center' }}>
      <h2 style={{ color: '#e2e8f0', fontSize: '2rem', marginBottom: '1rem' }}>Campaign</h2>
      <p style={{ color: '#94a3b8', fontSize: '1rem', maxWidth: '30rem', margin: '0 auto' }}>
        Questa sezione non è ancora stata implementata. Torna più tardi per
        gestire le tue campagne.
      </p>
    </div>
  );
};

export default Campaign;